- [ ] 全部 24 个 case 页面的 `:root` 颜色 token 使用语义化命名（`--color-canvas` / `--color-text-primary` / `--color-text-secondary` / `--surface-muted` / `--surface-subtle` / `--border-color`），名称和色值完全一致
- [ ] 不存在 `--ivory` / `--slate` / `--clay` / `--oat` / `--olive` / `--gray-100` / `--gray-150` / `--gray-300` / `--gray-500` / `--gray-700` / `--gray-800` / `--g100` / `--g200` / `--g300` / `--g500` / `--g700` 等阵营 B 原始色值命名
- [ ] 全部页面的 `--serif` / `--sans` / `--mono` 字体栈完全一致
- [ ] 全部页面的间距 token 使用 `--space-4` / `--space-8` / `--space-12` / `--space-16` / `--space-24` / `--space-32` / `--space-48` / `--space-64` 命名，遵循 8pt 网格
- [ ] 不存在 `--spacing-7` / `--spacing-10` / `--spacing-13` / `--spacing-29` / `--spacing-54` 等非标准间距 token
- [ ] 全部页面 body 字号为 15px、行高为 1.6、颜色为 var(--color-text-primary)、背景为 var(--color-canvas)
- [ ] 标准文档页面内容区域最大宽度为 860px（--page-max-width）
- [ ] 全部页面主容器使用 `.page` 类名，不存在 `.sheet` / `.wrap` 变体
- [ ] 全部页面面板、卡片、按钮等元素均无圆角（border-radius: 0）
- [ ] 全部页面面板、卡片等元素使用 1px solid var(--border-color) 边框，不存在 1.5px 或其他宽度
- [ ] 原阵营 B 的 21 个浅色页面已迁移至深色极简设计系统
- [ ] 原阵营 B 页面的功能组件（交互按钮、SVG 图形、表单元素等）在深色体系下正常显示
- [ ] Platform Eng 页面保留幻灯片布局但 token 和配色已统一为深色
- [ ] Debounced search 页面保留三栏布局但 token 和配色已统一为深色
- [ ] Empty state 页面保留分段控件但 token 和配色已统一为深色
- [ ] Card Variant Matrix 页面保留交互滑块但 token 和配色已统一为深色
- [ ] Consistent hashing 页面保留双栏布局和 `--sky` 扩展色（深色适配）但基础 token 已统一
- [ ] Deploy pipeline 页面保留流程图布局和 `--rust` 扩展色（深色适配）但基础 token 已统一
- [ ] Design System Reference 页面保留组件展示但 token 和配色已统一为深色
